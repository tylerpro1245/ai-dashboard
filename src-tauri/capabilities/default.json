{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default app capabilities (logging enabled).",
  "permissions": [
    "core:default",
    "log:default",
    "log:allow-log"
  ]
}
